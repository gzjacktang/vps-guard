# 领域文档

本项目采用单一上下文布局。

## 使用规则

- 探索代码前读取根目录 `CONTEXT.md`。
- 阅读与当前工作相关的 `docs/adr/`。
- 输出中使用 `CONTEXT.md` 定义的规范术语。
- 若方案与已有 ADR 冲突，必须明确指出，不得静默覆盖。
- 文件尚不存在时继续工作，由领域建模流程在需要时创建。

## 文件结构

```text
/
├── CONTEXT.md
└── docs/
    └── adr/
```
